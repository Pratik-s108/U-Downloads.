version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: pratiklanjewar4198040/u-downloader:latest
    container_name: u-downloader-container
    ports:
      - "3000:3000"
    volumes:
      - ./cookies.txt:/app/cookies.txt
      - tmp-data:/tmp
    restart: unless-stopped
    environment:
      - NODE_ENV=production
volumes:
  tmp-data:
